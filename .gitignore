# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
.uv/
.pytest_cache/
*.egg-info/

# Eval results (regenerated per run; not committed)
skills/**/evals/results-*.json
skills/**/evals/eval-results*.json

# Eval-produced artifacts at repo root (Sonnet subagents demonstrating a skill sometimes
# write real notebooks / scripts to cwd; reproducible from SKILL.md + scenario, don't track)
/*.ipynb

# Secrets and local environment overrides (never commit)
.env
.env.local
.python-version

# Logs and temp artifacts from uv / pytest / eval runs
*.log
*.tmp

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Project-local Claude Code state (per-machine, not for sharing)
.claude/

# Private skills (per-machine, not for sharing)
skills/teaching/brutal-grader/
