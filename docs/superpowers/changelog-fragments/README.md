# Batch changelog fragments

Per-batch changelog entries land here during parallel batch authoring (see `docs/superpowers/plans/2026-05-23-rcs-batch-creation-plan.md`). Batch 6 (integration) consolidates them into CHANGELOG.md and deletes them.

_This directory is empty except for this README between batch cycles._
