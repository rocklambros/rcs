### v6-batch-4: claude-code authoring meta — 2026-05-23

Skills shipped:
- `claude-code-meta/authoring-plugin` v0.1.0 — plugin authoring workflow for the Claude Code host: `.claude-plugin/plugin.json` manifest with required fields (name, version, description, license, author); `marketplace.json` entry pinning `source` to a SemVer tag or SHA; the auto-discovered `skills/` + `commands/` + `agents/` subdirectory convention vs. the explicitly-registered `hooks/` + `rules/` + `mcpServers` declarations; SemVer pin discipline (PATCH/MINOR/MAJOR semantics for plugin releases); lifecycle metadata for `post_install` / `pre_uninstall` / `post_update` scripts treated as elevated-permission artifacts; and the requirement to run the six-check pre-trust audit on every bundled hook and MCP server before publication. Hands off to `authoring-skill` for single-skill cases and to per-artifact skills (`authoring-tool-hook`, `writing-mcp-server-securely`, `writing-deny-allow-rules`) for the artifacts themselves (Σ 11, status: shipped). Slug renamed from the plan's `writing-claude-code-plugin` because the linter rejects the `claude` reserved word in slugs (precedent: v1-batch-5 `writing-claude-code-skill` → shipped as `authoring-skill`).
- `claude-code-meta/writing-mcp-server-securely` v0.1.0 — MCP server authoring with the six pre-trust checks baked in as design constraints from day one rather than retrofitted before publish: Check 1 (SPDX license + LICENSE file at repo root, matching pyproject.toml/package.json declaration); Check 2 (source-review-friendly code with no obfuscation, no eval/exec/dynamic-import-from-input, no base64-decoded payloads, no shell execution with unescaped user input); Check 3 (every outbound endpoint documented in the README + an auditable manifest field, target overridable via env var, no default-on telemetry); Check 4 (every dependency pinned in a committed lockfile, release tagged with SemVer, README install command pins to specific version); Check 5 (env-var-only secret loading with no config-file fallback, read once at startup, never logged, never echoed in errors, never included in tool-call results returned to Claude, least-privilege OAuth scope, fine-grained PAT over classic PAT); Check 6 (tools/list fixed at build time, no dynamic registration, no catch-all dispatch tool, explicit JSON Schema per tool, read-only separated from write tools). Includes a pre-publish self-audit checklist mirroring the consumer's `auditing-mcp-server-pre-trust` six checks (Σ 14, status: shipped).
- `claude-code-meta/authoring-tool-hook` v0.1.0 — Claude Code hook authoring across all 8 events (PreToolUse, PostToolUse, SessionStart, SessionEnd, UserPromptSubmit, Stop, SubagentStop, Notification) with per-event stdin payload schemas and stdout response contracts; the explicit fail-open vs. fail-closed failure-mode discipline (security gates default fail-closed, observability hooks default fail-open, ambiguity is forbidden); hook-as-elevated-permission-artifact security review (no unescaped shell interpolation, no eval/exec/dynamic import from input, absolute or repo-relative script path never PATH-resolved, audit-log every decision); timeout discipline for HTTP-egress hooks paired with client-side caching of policy decisions to avoid an HTTP call on every tool invocation; and the matcher-scoping discipline (smallest matcher that meets the need, never unscoped on every tool). Slug renamed from the plan's `writing-claude-code-hook` because the linter rejects the `claude` reserved word. (Σ 12, status: drafting — see Notes below).
- `claude-code-meta/writing-deny-allow-rules` v0.1.0 — `.claude/rules/*.md` authoring with one rule per file, frontmatter fields (name, decision, tool, matcher, severity) and required body sections (Rationale, Allowed alternatives, Audit notes); the matcher-variant discipline that catches `--force` AND `-f` AND `--force-with-lease` rather than a brittle exact-string match; the multi-file composition pattern for complex policy (e.g., the SSH + /etc + /etc/cron.d example split across three single-purpose files); the documented precedence model (more-specific wins over less-specific; deny wins over allow at the same specificity; user > project > plugin scope precedence); and the rule-vs-hook (declarative vs. executor) and rule-vs-CLAUDE.md (enforced vs. advisory) distinctions. Hands off to `update-config` for trivial single-tool allowlists where settings.json `permissions.allow` is the right artifact (Σ 13, status: shipped).
- `claude-code-meta/writing-decision-trees-as-skills` v0.1.0 — meta skill that converts existing decision-tree expertise (stats-test selection trees, vuln-triage flowcharts, model-tier dispatch matrices, troubleshooting trees) into deterministic walk-the-tree skills: numbered-step predicates mirroring the tree structure, explicit branches per predicate value (no implicit defaults), per-predicate preconditions naming the data required to evaluate (so the skill requests input rather than guesses), an anti-shortcut clause that binds the skill to walking from Step 1 even when the user names a leaf in the question, and explicit cycle-handling via one of three documented options (state-enrichment, state-machine, or revisit-cap) with hand-off to brainstorming/premortem for open-judgment domains that resist tree encoding (Σ 13, status: shipped).

Eval methodology: Sonnet-only in-session validation per PRAGMATIC discipline. 15 dispatches (5 skills × 3 scenarios — one combined dispatch per skill running all 3 scenarios as separate completions to amortize overhead). Total rubric items scored: 45.

Eval results (intent-matched against rubrics):
- `authoring-plugin`: happy 3/3, edge 3/3, anti 3/3 — pass all thresholds (`shipped`)
- `writing-mcp-server-securely`: happy 3/3, edge 3/3, anti 3/3 — pass all thresholds (`shipped`)
- `authoring-tool-hook`: happy 3/3, edge 2/3, anti 3/3 — edge-case threshold (3/3) missed; demoted to `drafting`
- `writing-deny-allow-rules`: happy 3/3, edge 3/3, anti 3/3 — pass all thresholds (`shipped`)
- `writing-decision-trees-as-skills`: happy 3/3, edge 3/3, anti 3/3 — pass all thresholds (`shipped`)

Full 3-model (Haiku / Sonnet / Opus) validation deferred to a future re-run.

Notes:
- Two slug renames from the plan's planned slugs because the frontmatter linter rejects the `claude` reserved word in slugs: `writing-claude-code-plugin` → `authoring-plugin`; `writing-claude-code-hook` → `authoring-tool-hook`. Precedent: v1-batch-5 shipped `writing-claude-code-skill` as `authoring-skill` and `auditing-claude-md-hierarchy` as `auditing-instruction-hierarchy` for the same reason. The Batch 6 (v6-integration) pass will remove the original planned-skill rows from `skills/claude-code-meta/README.md` and add the shipped rows under the new slugs.
- `authoring-tool-hook` shipped as `drafting` (not `shipped`) because the Sonnet eval scored 2/3 on the edge-case scenario (PreToolUse hook with HTTP egress to a policy server). The skill body teaches client-side caching of policy decisions in Step 5 (Example 2), but the Sonnet completion emphasized scoped matcher + timeout discipline + the fail-open/fail-closed decision without surfacing the caching guidance. The teaching content is correct; the eval reveals the Workflow ordering does not surface the caching pattern early enough. Re-validation pass should either reorder the Workflow steps to put caching alongside the timeout choice, or add an explicit "caching is required for HTTP-egress hooks" rubric to the body language. Status will be re-evaluated after the v6-integration pass or in a follow-up commit.
- The 5 skills compose deliberately: `authoring-plugin` is the bundling discipline; `writing-mcp-server-securely`, `authoring-tool-hook`, `writing-deny-allow-rules`, and `writing-decision-trees-as-skills` are the per-artifact authoring disciplines that every bundled artifact must pass. `writing-mcp-server-securely` mirrors the consumer's `auditing-mcp-server-pre-trust` six-check pattern from the publisher side; `authoring-tool-hook` and `writing-deny-allow-rules` separate the executor from the declarative-data side; `writing-decision-trees-as-skills` is a meta-skill that pairs with `authoring-skill` for skill authors who have explicit-tree domain expertise to encode.
- One side effect to surface for the user: the Sonnet subagent running the `writing-deny-allow-rules` eval (Scenario 2) actually wrote 4 rule files to `~/.claude/rules/` (`deny-ssh-key-writes.md`, `deny-etc-writes-broad.md`, `allow-etc-cron-writes.md`, `deny-force-push-on-main.md`) during the simulated walk. The files were not requested by the user and were left in place rather than auto-deleted (per the harness rule against unconfirmed writes outside the working directory). Recommend reviewing those 4 files and removing them if they were not desired.
