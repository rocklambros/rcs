# ML / Data Science Track

For data scientists, ML engineers, applied-ML practitioners, and stats students.

This track encodes disciplines that recur across model-building work: statistical-test selection from data characteristics, classifier and regression evaluation, calibration, leakage firewalls, baseline-model discipline, regression diagnostics, fine-tuning audits, and RAG retrieval evaluation.

## Shipped skills

| Skill | What it does | When to use | Σ |
|---|---|---|---|
| [`reporting-effect-sizes`](reporting-effect-sizes/) | Selects the appropriate effect-size metric per test family (Cohen's d / dz, Cliff's δ, rank-biserial, OR, partial η², adj-R²) + 95% CI + direction sentence; refuses bare-p-value reports | When reporting any hypothesis-test result, writing stats homework / papers / dashboards, or seeing 'p < 0.05 so significant' without an effect size | 19 |
| [`evaluating-binary-classifiers`](evaluating-binary-classifiers/) | ROC + PR + calibration + CM + threshold sweep + bootstrap 95% CI from `(y_true, y_pred_proba)`; refuses bare accuracy on imbalanced data; refuses default 0.5 threshold | A trained binary classifier needs scoring; class imbalance suspected; only accuracy reported on imbalanced data; threshold selection needed | 19 |
| [`selecting-statistical-test`](selecting-statistical-test/) | Decision-tree walk from data characteristics (sample count, paired/independent, scale, distributional assumptions) to recommended test, naming the gating assumption check | User has a hypothesis + data and needs to commit to a test before running it | 18 |
| [`checking-test-assumptions`](checking-test-assumptions/) | Per-test assumption checks (Shapiro / Levene / QQ / residual diagnostics) with pass/fail verdicts and consequences | Before running any parametric test, after running one to validate, or when asked 'are the assumptions met?' | 18 |
| [`auditing-train-test-split`](auditing-train-test-split/) | Leakage / stratification / group-aware / temporal-order audit of a train/test split | User creates a split; accuracy looks suspiciously high; same ID in train+test; time-series random-split; or group-aware data is row-split | 18 |
| [`building-baseline-models`](building-baseline-models/) | 3-rung baseline ladder (Dummy / Linear / RandomForest) fit on the SAME train/test split and SAME metric as the final model; refuses to certify a complex-model metric without baseline comparison | About to fit gradient boosting / deep learning / any complex model; a metric is reported without comparison context; "is `<metric>` good?" without a baseline | 17 |
| [`evaluating-regression-models`](evaluating-regression-models/) | RMSE + MAE + R² + adjusted-R² + residual plots (residuals-vs-fitted, QQ, histogram) + k-fold CV (`mean ± SD`) + Cook's D; refuses R² alone; recommends walk-forward CV for time-series | A regression model needs scoring; only R² is reported without RMSE/MAE; residual diagnostics needed; CV missing | 17 |
| [`auditing-prompt-token-budget`](auditing-prompt-token-budget/) | Tokenizes an Anthropic API request (system + tools + tool_results + every turn), classifies blocks into stable-prefix vs volatile-suffix, recommends `cache_control` breakpoint placement at the end of the stable region with explicit `"ttl": "1h"`, projects write-vs-read cost vs no-cache cost, and prescribes cache-hit-rate telemetry; refuses to engage on prompts under ~500 tokens | Per-call cost is climbing, latency on first turns is too high, request is approaching the 200k window, shipping a new agentic / RAG / multi-turn workflow, or the user asks where to put cache_control | 16 |
| [`evaluating-multiclass-classifiers`](evaluating-multiclass-classifiers/) | Full multi-class classifier report: per-class precision / recall / F1 with support, macro + weighted + micro aggregations with cost-model justification, K x K confusion matrix in counts and row-normalized views, top-k accuracy where informative, one-vs-rest ROC and PR per class, multi-class log loss / Brier / Cohen's kappa, bootstrap 95% CI on macro-F1 and rare-class F1; refuses bare overall accuracy under imbalance and hands off to evaluating-binary-classifiers on 2-class tasks | A trained classifier has 3 or more labels and needs to be scored; overall accuracy is reported on a 3+-class task with imbalance; or the user is unsure whether macro / weighted / micro aggregation matches the deployment cost model | 16 |
| [`tuning-classification-threshold`](tuning-classification-threshold/) | Domain-aware threshold selection with five selectors (F-beta, TPR-at-fixed-FPR, precision-at-fixed-recall, cost-weighted, Youden's J), enforced validation-slice tuning + held-out-test reporting separation, mandatory calibration check before applying any probability threshold; refuses default 0.5 and refuses to pick threshold on the test set | A trained binary classifier is moving to deployment; a metric is reported at threshold 0.5 without justification; a hard FPR / precision / recall budget exists; or there is FN-vs-FP cost asymmetry (fraud, security, medical screening) | 16 |
| [`running-chollet-ratio-check`](running-chollet-ratio-check/) | Chollet samples-per-word ratio decision rule for text / sequence classification model-family selection: ratio < 1500 favors TF-IDF + linear (BoW); 1500-15000 is a transition zone for small RNN / 1D-CNN; > 15000 supports Transformer / pretrained fine-tune; includes domain-pretraining threshold adjustment and per-class minimum sanity check; always requires a BoW baseline comparison; refuses to recommend a Transformer below the threshold without domain-pretraining justification and refuses to apply the ratio to non-sequence data | A user is starting a text classification project, picking between TF-IDF baseline and a deep model, or about to fine-tune a Transformer on a small labeled dataset where the ratio test predicts the deep model will overfit | 16 |
| [`interpreting-conflicting-tests`](interpreting-conflicting-tests/) | Adjudicates between two statistical tests on the same data that disagree (paired-t vs Wilcoxon, two-sample-t vs Mann-Whitney, McNemar vs exact binomial sign, chi-squared vs Fisher) by building an assumption-status table, picking the test whose assumptions hold (NOT the smaller p-value), and committing to one primary with the matched effect-size + 95% CI; refuses 'mixed evidence' hand-waving and p-hacking via test-shopping | Two tests on the same data disagree, user asks which p-value to report, reviewer flagged conflicting results in a manuscript, or user wants to keep running tests until one is significant | 16 |
| [`generating-data-dictionary`](generating-data-dictionary/) | Generates a per-column data dictionary (type, null %, unique count, range, semantic class, role, PII flag, anomaly notes) for a tabular dataset | When receiving a fresh client / partner / vendor dataset, before fitting any model on an undocumented dataset, or as the first step before broader data-quality auditing | 15 |
| [`analyzing-regression-diagnostics`](analyzing-regression-diagnostics/) | Six-step diagnostic battery on a fitted OLS / GLS / GLM regression (linearity, residual Normality, homoscedasticity, autocorrelation, leverage + Cook's D, multicollinearity / VIF) with per-diagnostic verdict + remediation; refuses to run on tree / kernel / neural models | A linear or GLM regression has been fit and inference (coefficients, p-values, CIs) is about to be reported; surprising results need investigation; or 'are my regression diagnostics OK?' | 14 |
| [`enforcing-leakage-firewall`](enforcing-leakage-firewall/) | Four-check leakage defense for supervised pipelines: LOFO sweep (target-encoded / post-event features), hub-firewall for crosswalk / multi-source data, group-aware splitting for multi-row-per-entity data, no-row-in-two-splits row-hash invariant; refuses to trust a held-out metric until all four checks pass on the relevant subset | A dataset is assembled from multiple sources (crosswalk, registry merge); has natural group structure (patient_id, user_id, customer_id); the held-out metric looks suspiciously high; or after any join / merge step that could create cross-split row identity | 14 |
| [`comparing-models-fairly`](comparing-models-fairly/) | Paired statistical tests for head-to-head model comparison: McNemar / DeLong for two classifiers on the same test set, paired-t or Wilcoxon signed-rank for k-fold CV, Friedman + Nemenyi or Holm-Bonferroni correction for 3+ models; requires effect-size + bootstrap CI alongside p-value and distinguishes statistical from operational significance; refuses unpaired tests on per-fold metrics and refuses to skip multiple-comparison correction | Two or more candidate models need a deployment winner; two metrics differ but their CIs overlap; the user reports a 'better' model without a paired test; or 3+ models are being compared without a correction plan | 14 |
| [`evaluating-rag-retrieval`](evaluating-rag-retrieval/) | Staged RAG eval from a golden Q-A set. Retrieval metrics (recall@k, MRR, nDCG@k) separated from generation metrics (faithfulness, answer relevance, context utilization scored on retrieval hits) plus a retrieval-vs-generation failure-attribution 2x2 driving the next experiment; refuses single aggregate scores that conflate stages | A RAG pipeline needs scoring, the user reports "RAG feels worse than direct LLM" but cannot localize the stage, a chunking / embedding / reranker / top-k change needs A/B comparison, or the generation looks wrong but retrieval looks fine | 14 |
| [`profiling-llm-cost`](profiling-llm-cost/) | Per-call → per-task → per-cohort LLM cost rollup from a logged trace (input / cached_input / output tokens × pricing snapshot), cache-hit-rate trend, attribution slices by model / step / route / cohort, baseline-window delta, top-3 cost drivers, and recommendations ranked by $ saved per engineering hour; refuses cost-cutting actions without a measured baseline | A production LLM bill spikes without proportional traffic growth, prompt-cache hit rate drops, an agentic system's $-per-task is unknown, or a budget gate is needed before scaling | 14 |
| [`recommending-model-tier`](recommending-model-tier/) | Decomposes a pipeline into discrete LLM calls and recommends Haiku 4.5 / Sonnet 4.6 / Opus 4.7 per task based on reasoning depth, latency budget, cost target, safety-critical status, and same-family-cache lineage; pairs every downgrade with an escalation rule and an eval; refuses to engage on policy-locked single-tier deployments | Designing a new LLM pipeline, single-tier deployment is too slow / expensive, choosing tier for a specific task, responding to a benchmark that disagrees with production, or shipping a multi-stage agent | 14 |
| [`running-power-analysis`](running-power-analysis/) | Prospective (a-priori) power / n / MDE calculation for any planned test (t / ANOVA / chi-squared / correlation / regression / logistic) with required effect-size provenance (pilot / literature / SESOI / Cohen rule-of-thumb) and sensitivity check; refuses post-hoc / observed power and routes to TOST or CI interpretation | Designing a study, writing a grant / pre-registration / IRB, choosing between designs, or being asked 'what was the power?' on a non-significant result | 13 |
| [`writing-model-cards`](writing-model-cards/) | Mitchell-2019 model card with AIBOM addendum: intended use + out-of-scope use, factors / subgroups, metrics with 95% CIs disaggregated by subgroup, evaluation + training data provenance with collection-window + license + known biases, concrete deployment-specific harms with paired mitigations, dependencies + versions + hashes for AI supply-chain traceability; refuses vague 'may have biases' language and refuses to author a card for a throwaway research notebook | A model is moving to production deployment; an EU AI Act / NIST AI RMF / ISO 23894 compliance review requests documentation; an open-weights release; or a customer / regulator / auditor requests a model card | 13 |
| [`selecting-embedding-model`](selecting-embedding-model/) | Three-axis embedding-model comparison (intrinsic similarity correlation on domain pairs + extrinsic retrieval recall@k / MRR / nDCG on a golden Q-A set + operational cost including per-query, index-build amortization, latency, and dim-driven index size) with bootstrap-CI tie-detection and operational tiebreaker; refuses recommendations from public leaderboards alone | Starting a new RAG / semantic-search / dedupe pipeline, considering swapping the embedding model, deciding between two candidates that "feel similar" on MTEB, or trading off a high-tier paid model against a self-hosted alternative | 13 |
| [`packaging-model-for-deployment`](packaging-model-for-deployment/) | Packages a trained model as a versioned artifact + input/output schema + manifest + smoke test (joblib / ONNX / TorchScript per stack), refusing to certify a model as deploy-ready without a smoke test that round-trips through the saved artifact | A notebook-trained model is about to be served behind an API / batch / streaming inference, the only artifact is a bare pickle, or the user is choosing between joblib / ONNX / TorchScript / framework-native serialization | 12 |
| [`scaffolding-pytorch-training-loop`](scaffolding-pytorch-training-loop/) | Production PyTorch training loop scaffold. Seed + AMP + grad-clip + LR schedule + early stop + atomic-write full-state checkpoint + W&B resume | When starting a new PyTorch training script, when an existing loop lacks resume-after-preemption, or when same-seed results vary run-to-run | 12 |
| [`running-hyperparameter-sweep`](running-hyperparameter-sweep/) | Disciplined Optuna / Ray Tune sweep. Search-space design, TPE + ASHA, sweep-vs-retrain budget split, seed-stratified top-K retrain, boundary alarms, single-shot test eval | When manual tuning has stalled, when planning compute for an expensive model, or when a sweep result looks suspiciously close to defaults | 12 |
| [`running-eval-before-after-finetune`](running-eval-before-after-finetune/) | Paired before/after fine-tune evaluation with appropriate paired test per metric family (McNemar / paired-t / Wilcoxon / Cochran's Q / Friedman), gating assumption check, effect size + 95% CI, power-check, and certify/do-not-certify verdict | When a candidate fine-tune needs to be certified vs the base checkpoint with evidence stronger than a single-number metric delta | 12 |
| [`monitoring-data-drift`](monitoring-data-drift/) | Per-feature data-drift monitor. PSI / KS for continuous, Jensen-Shannon / chi-squared for categoricals; reference window + baseline-noise-calibrated thresholds + per-cohort attribution + root-cause hierarchy before retraining is recommended | Production ML model has accumulated ≥ one reference window of live inference traffic AND performance erosion / pipeline change / range shift / regulator-requested attestation requires per-feature drift attribution | 11 |
| [`monitoring-prediction-drift`](monitoring-prediction-drift/) | Prediction-side drift monitor. Tracks score distribution, calibration over time (reliability curve, slope/intercept, Brier), per-segment performance erosion with bootstrap 95% CI on labeling-delay-aware windows; recommends Platt / isotonic recalibration before retraining when calibration alone has moved | Production model has accumulated live predictions plus labels for at least one labeling-delay-aware window, OR performance erodes while feature drift looks stable, OR per-segment fairness / accuracy tracking is required | 11 |
| [`auditing-sft-dataset`](auditing-sft-dataset/) | Pre-training audit of an SFT corpus across schema validity, chat-template conformance, length distribution, exact + MinHash duplicate detection, train-vs-eval n-gram leakage, PII against a user-supplied policy, and a label-quality review sample | After assembling an SFT corpus and before launching fine-tuning, especially for scraped / synthetic / preference-pair-flattened data | 11 |
| [`auditing-synthetic-data-utility`](auditing-synthetic-data-utility/) | TSTR / TRTS / TRTR utility audit for tabular SDG output with bootstrap 95% CI on the utility ratio, per-marginal KS / Wasserstein, pairwise-correlation Frobenius gap, and a use-as-real / use-with-caveats / reject verdict; refuses to certify utility without a held-out REAL test set | About to train, share, publish, or benchmark a tabular synthetic dataset generated by SDV / CTGAN / TVAE / Synthpop / Gretel / Mostly AI / DP-synth and needing to know whether it can substitute for real data on a downstream task | 11 |
| [`building-data-dictionary-with-consent-class`](building-data-dictionary-with-consent-class/) | Per-field data dictionary with consent class (collected / inferred / derived / public / synthetic), DSR scope per right per regime (GDPR / CCPA / HIPAA), Article-9 sensitive flagging, lineage / propagated_to cascade, and orphan-field detection for data-minimization compliance | Preparing for GDPR / CCPA / HIPAA DSR readiness, building a processing inventory or DPIA, classifying ML-derived features (embeddings, risk scores, predictions) for privacy scope, or auditing an existing schema before a privacy review | 11 |
| [`building-rollback-plan`](building-rollback-plan/) | Model rollback runbook. Versioned artifact store + deterministic triggers + oncall as decision authority + named control-plane reversal mechanism + state reconciliation + smoke-test re-entry gate | A model is being promoted to production, an oncall asks what to do if the new model breaks, state migrations between versions are at issue, or the deployment has stateful behavior (caches, sessions, downstream writes) | 10 |
| [`auditing-inference-latency-budget`](auditing-inference-latency-budget/) | Real-time inference latency audit against a stated SLO. P50 / P95 / P99 end-to-end and per-stage, slow-vs-fast comparison for tail-driver attribution, regresses on input-shape covariates, classifies tail cause, and ranks optimizations by (ms saved at P99) / (engineering hours) | Real-time inference service breaching or close to breaching P95 / P99 SLO, OR latency tail is unexplained, OR input-shape distribution shift suspected, OR pre-launch SLO sizing exercise needs a measured per-stage profile | 10 |
| [`writing-finetune-spec-sheet`](writing-finetune-spec-sheet/) | Fine-tune spec sheet across seven required sections. Base checkpoint with immutable revision SHA, data provenance, training recipe, paired eval evidence, limitations, license reconciliation, and intended-use + out-of-scope-use | When publishing a fine-tune to a registry, handing off to a deploying team, or submitting for regulated model-risk review | 10 |
| [`auditing-synthetic-data-leakage`](auditing-synthetic-data-leakage/) | Empirical privacy audit for tabular synthetic data. Shadow-model MIA with bootstrap CI, DCR / NNDR distance audits, near-duplicate detection, per-attribute disclosure, and a publish / publish-with-DP / restrict / withhold verdict per sensitivity class; treats CI upper bound as the operative number on sensitive-data classes | About to share, publish, license, or release synthetic tabular data derived from PHI / PII / financial / customer real data, or comparing SDG techniques on an empirical privacy axis alongside utility | 10 |
| [`building-canary-rollout`](building-canary-rollout/) | Staged traffic split with pre-committed business + technical + per-cohort guardrail thresholds and a deterministic auto-rollback trigger wired before the first flip | A new model version is about to replace a production model, a teammate proposes a 0%-to-100% overnight flip, per-cohort regression risk is at issue, or offline metrics improved but online behavior is unknown | 9 |
| [`auditing-chunking-strategy`](auditing-chunking-strategy/) | Audits a RAG pipeline's chunking strategy via a chunk_size × overlap sweep against a held-out QA set with source-span attribution, then recommends a splitter (paragraph-aware, semantic, section-aware, AST-aware) matched to the document structure. Optimizes answer-coverage, not bare recall@k. | Retrieval recall is dropping on long documents, chunks visibly cut answers in half, or chunk_size/overlap was copied from a tutorial without a sweep. | 13 |
| [`auditing-model-fairness`](auditing-model-fairness/) | Audits a trained classifier for disparate treatment across protected attributes (race, sex, age, etc.) using equal-opportunity, demographic-parity, calibration-within-group, four-fifths rule, and intersectional cuts with bootstrap 95% CIs; names Kleinberg-Mullainathan-Raghavan impossibility trade-offs explicitly; refuses to declare a model "fair" from a single metric and refuses to silently drop low-power groups | A consequential ML system decides outcomes for people (hiring, lending, healthcare triage, criminal-justice scoring, content moderation), pre-deployment review is required, or a regulator (EU AI Act, NYC Local Law 144, EEOC four-fifths, ECOA) requires a disparate-impact analysis | 12 |
| [`auditing-deep-learning-overfit`](auditing-deep-learning-overfit/) | Audits a deep-learning training run by walking a 7-step decision tree (confirm signature → rule out underfit → rule out shift → rule out label noise → check weight-norm trajectory → diagnose → remediate in priority order), distinguishing classic overfit from plateau / shift / label-noise / underfit; refuses the "add dropout" reflex without ruling out shift and label noise first; refuses to apply DL-specific diagnostics to classical ML | A deep network's val loss is rising while train loss is dropping, a model that "looked good" generalizes poorly, deciding whether to add regularization vs collect more data, or triaging a failed training run before further hyperparameter tuning | 12 |
| [`building-rag-eval-set`](building-rag-eval-set/) | Builds a golden Q-A eval set for RAG with three splits (calibration, never-viewed held-out test, adversarial) where every non-absent-topic row carries source-doc + source-span attribution and every Q-A passes a human-review gate. Locks the set with a dataset hash and SemVer. | Starting RAG evaluation from scratch in a custom domain where no public benchmark fits, or preparing the eval foundation for a chunking / embedding-drift / retrieval-quality audit. | 12 |
| [`building-conformal-prediction-set`](building-conformal-prediction-set/) | Builds a split-conformal prediction set or interval around any black-box scorer with a distribution-free finite-sample marginal coverage guarantee, including the train / calibration / test discipline, the finite-sample-corrected quantile, and the exchangeability red-flag checklist | When the user needs calibrated uncertainty from a black-box classifier or regressor, wants guaranteed prediction-set coverage, or has a downstream consumer that requires a documented coverage rate | 11 |
| [`generating-shap-explanations`](generating-shap-explanations/) | Generates SHAP feature attributions with the right explainer per model class (TreeExplainer for tree ensembles, DeepExplainer/GradientExplainer for nets, KernelExplainer for black-box), a deliberately-chosen stratified background dataset sized ≥ 5× n_features, per-instance waterfall + global mean(\|SHAP\|) bar + beeswarm, and a mandatory stability check across 3 background resamples; refuses to run SHAP on linear/logistic models (coefficients ARE the closed-form attribution) | A stakeholder asks why the model predicted a specific case, global feature-importance is needed, regulatory explanations are required (EU AI Act, FCRA adverse-action, GDPR Article 22), or a surprising prediction needs attribution | 11 |
| [`auditing-embedding-drift`](auditing-embedding-drift/) | Audits a deployed retrieval system for embedding drift between two cohorts via per-dim Jensen-Shannon divergence, centroid cosine distance, and intra-cohort distance shift, with bootstrap 95% CIs and attribution to new content categories, upstream-data shift, or provider-side model re-versioning. | Retrieval recall eroding without a code change, the embedding model has been re-versioned, or comparing two embedding cohorts over a time / source / model axis. | 11 |
| [`running-bayesian-workflow`](running-bayesian-workflow/) | Walks the full Bayesian workflow (weakly-informative priors → prior-predictive check → NUTS sampling → five-check diagnostic gate → posterior-predictive check → LOO comparison → credible-interval reporting), including the CPU-pin determinism block that prevents cross-platform posterior drift | When the user fits a Bayesian / NUTS model in PyMC / NumPyro / Stan / brms, reports divergences or low ESS, wants posterior intervals, or hands over a hierarchical design | 10 |
| [`analyzing-causal-dag`](analyzing-causal-dag/) | Walks an observational-study DAG, classifies each node (confounder / mediator / collider / IV), applies the backdoor criterion to nominate a sufficient adjustment set, screens out post-treatment variables that would bias the estimate, and pairs the point estimate with an E-value sensitivity analysis | When the user is about to claim causal effect from observational data, asks whether a covariate should be adjusted for, reports a Simpson-paradox sign flip, or hands over a study where confounding is plausible | 9 |
| [`evaluating-ood-detection`](evaluating-ood-detection/) | Evaluates an OOD detector on a held-out in-distribution set plus REQUIRED near-OOD and optional far-OOD sets, computing AUROC + AUPR-out + FPR-at-95-TPR per OOD set separately with bootstrap CIs; method selection table (MSP / energy / Mahalanobis / KNN / ODIN) with trade-offs; refuses far-OOD-only evaluation (false sense of safety) and refuses to apply in closed-world deployments where every input maps to a training class | A classifier is deployed open-world where novel-class inputs arrive, a safety reviewer requires the model to "know what it doesn't know", a rejection/abstain pathway is being added, or comparing two OOD detectors on the same backbone | 9 |
| [`running-adversarial-perturbation-suite`](running-adversarial-perturbation-suite/) | FGSM + PGD-20 + AutoAttack-standard suite under a declared threat model (norm, ε with units, white-box/black-box, targeted/untargeted) with per-attack robust-accuracy table, strict monotone-strength sanity check, tabular feasibility filter, and saved adversarial examples; refuses to apply to LLM prompt-injection (no continuous gradient. Hands off to security/running-prompt-injection-eval) | A vision or tabular classifier needs an adversarial-robustness number under a stated ε, a regulator or customer requests a robustness measurement, or high clean accuracy is reported on a security-critical task with no robustness data | 8 |
| [`applying-differential-privacy`](applying-differential-privacy/) | DP application workflow. Declare the threat model (membership-inference / attribute-disclosure / reconstruction / query-aggregation), justify (ε, δ) against the threat and n, pick mechanism (Gaussian / Laplace / DP-SGD / Exponential), compute noise scale, track composition with an RDP accountant, empirically validate with LiRA, and emit the canonical DP statement; refuses ε ≥ 10 marketing claims and refuses to apply DP when k-anonymity is sufficient | An ML training run or analytic query workload requires a formal privacy guarantee (HIPAA / GDPR / regulatory), a learned reward / k-anonymity proposal is insufficient for the threat model, or membership inference is a credible attack against a fine-tuned model on private data | 8 |
| [`auditing-rlhf-reward-hacking`](auditing-rlhf-reward-hacking/) | Six-probe RLHF / DPO / RLAIF audit (length-bias, sycophancy, formatting, refusal-substitution, persuasion-over-correctness, reward-boundary exploitation) anchored on reward-model vs held-out-preference win-rate divergence as the headline signal; alignment-tax measurement on held-out task benchmarks; saves boundary-exploit gallery for human inspection; refuses to apply to pure SFT models (no reward model to hack. Hands off to running-eval-before-after-finetune and auditing-sft-dataset) | A preference-tuned policy (RLHF / DPO / RLAIF) is being gated for promotion, the post-tuned model scores higher on the reward model but worse on held-out preference data, or an alignment-tax measurement is requested | 7 |

## Planned skills

### Statistical / mathematical reasoning

_All shipped in v7-batch-1 (see Shipped skills table)._

### ML / DL hygiene

_All shipped in v7-batch-2 (see Shipped skills table)._

### RAG / fine-tuning / MLOps

_All shipped in v7-batch-2 / v7-batch-3 (see Shipped skills table)._

### Synthetic data + privacy

_Shipped in v7-batch-4 (see Shipped skills table)._

### Scaffolding

_Three originally-planned scaffolding skills shipped in v6 under the `workflow/` track rather than `ml-datasci/`, because they apply to any project rather than ml-datasci specifically. See [`workflow/scaffolding-ml-research-notebook`](../workflow/scaffolding-ml-research-notebook/), [`workflow/scaffolding-grad-school-pset`](../workflow/scaffolding-grad-school-pset/), and [`workflow/scaffolding-llm-eval-harness`](../workflow/scaffolding-llm-eval-harness/)._

## Cross-track references

- For seed hygiene, train/test split discipline, and reproducibility, see `workflow/`.
- For research discipline (premortems, pre-registration), see `workflow/`.
- For teaching variations of these skills (rubrics, walkthroughs), see `teaching/` (planned).
