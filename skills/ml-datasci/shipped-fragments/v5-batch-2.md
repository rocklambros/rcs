| [`monitoring-data-drift`](../monitoring-data-drift/) | Builds a per-feature data-drift monitor for a deployed ML model — PSI / KS for continuous features, Jensen-Shannon / chi-squared for categoricals, with a documented reference window, alert thresholds calibrated against empirical baseline noise (not hard-coded industry bands), per-cohort attribution, and root-cause hypothesis hierarchy (instrumentation bug → cohort-mix shift → real population drift); refuses to set fixed thresholds without baseline-noise calibration and refuses on pre-deployment systems with no live inference traffic | Production ML model has accumulated ≥ one reference window of live inference traffic AND performance erosion / pipeline change / range shift / regulator-requested attestation requires per-feature drift attribution | 11 |
| [`monitoring-prediction-drift`](../monitoring-prediction-drift/) | Builds a prediction-side drift monitor — tracks the predicted-score distribution, calibration over time (reliability curve, slope and intercept, Brier score), and per-segment performance erosion (AUC / F1 / precision-recall by cohort with bootstrap 95% CI) on labeling-delay-aware evaluation windows; recommends Platt or isotonic recalibration before retraining when calibration alone has drifted; refuses to evaluate on windows where labels are not yet mature and refuses on pre-deployment systems with no live predictions | Production model has accumulated live predictions plus labels for at least one labeling-delay-aware window, OR performance erodes while feature drift looks stable, OR per-segment fairness / accuracy tracking is required | 11 |
| [`auditing-inference-latency-budget`](../auditing-inference-latency-budget/) | Audits the inference-latency budget for a deployed real-time ML model against a stated SLO — measures P50 / P95 / P99 end-to-end and per-stage (tokenize / feature_lookup / preprocess / model_forward / postprocess / serialize), runs slow-vs-fast comparison (P95-P99 vs P40-P50) to identify the tail driver, regresses per-stage latency on input-shape covariates (sequence length, batch size, fanout), classifies the tail cause (input-shape variance / cold-start / contention / external dependency / cache miss / fanout depth), and ranks optimizations by (ms saved at P99) / (engineering hours) with quality-risk noted; refuses to recommend fixes without measured per-stage attribution and refuses on batch / offline workloads where latency SLO does not apply | Real-time inference service breaching or close to breaching P95 / P99 SLO, OR latency tail is unexplained, OR input-shape distribution shift suspected, OR pre-launch SLO sizing exercise needs a measured per-stage profile | 10 |
