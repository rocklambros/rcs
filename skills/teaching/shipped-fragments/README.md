# teaching track — shipped-fragments

Per-batch shipped-skills fragments land here during parallel batch authoring (see [`docs/superpowers/plans/2026-05-23-rcs-batch-creation-plan.md`](../../../docs/superpowers/plans/2026-05-23-rcs-batch-creation-plan.md)).

Each batch that ships a skill into the **teaching** track writes `batch-N.md` here. Batch 6 (integration) consolidates fragments into [`../README.md`](../README.md) and deletes them.

Fragment format (one or more rows of):

```markdown
| [`<slug>`](../<slug>/) | <what it does, one line> | <when to use, one line> | <Σ> |
```

_This directory is empty (apart from this README) between batch cycles._
